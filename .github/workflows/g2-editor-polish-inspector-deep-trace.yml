name: G2 Editor Polish Inspector Deep Trace

on:
  pull_request:
    branches:
      - g2/integration-beta

permissions:
  contents: read

jobs:
  trace:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    env:
      QT_QPA_PLATFORM: offscreen
      PYTHONUNBUFFERED: '1'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
      - name: Install Linux Qt prerequisites
        if: runner.os != 'Windows'
        shell: bash
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends libegl1 libgl1 libxkbcommon-x11-0 libxcb-cursor0
      - name: Install runtime Linux
        if: runner.os != 'Windows'
        shell: bash
        run: python -m pip install -e '.[dev,graphics2]'
      - name: Install runtime Windows
        if: runner.os == 'Windows'
        shell: pwsh
        run: python -m pip install -e '.[dev,graphics2]'
      - name: Run deep trace
        run: python -m pytest -s -q tests/test_graphics2_image_inspector_deep_trace_v2.py
